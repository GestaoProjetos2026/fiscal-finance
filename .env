# ==============================================================
# CONFIGURAÇÕES DO AMBIENTE (Fiscal Finance)
# ==============================================================

# FISC-MOD5-02: Chave real para produção (não use a padrão de dev)
SECRET_KEY=fisc_finance_2026_prod_secret_key_8822

# FISC-MOD5-01: Define que o sistema está em produção para ativar HTTPS
FLASK_ENV=production

# FISC-MOD5-02: Caminho para o banco de dados
# Usaremos DATABASE_URL para manter o padrão que o Docker reconhece
DATABASE_URL=data/app.db
